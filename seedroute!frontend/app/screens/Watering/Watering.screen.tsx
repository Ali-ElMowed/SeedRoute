import React from "react";
import { Text, View, StyleSheet, Image, ScrollView } from "react-native";
interface homeScreenProps {
  navigation: any;
}
const Watering = (props: homeScreenProps) => {

  return (
    <ScrollView>
      <View >
      </View>
    </ScrollView>
  );
};

const styles = StyleSheet.create({

})
export default Watering;